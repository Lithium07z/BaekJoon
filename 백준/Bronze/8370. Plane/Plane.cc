#include <iostream>
#include <array>
#include <algorithm>

using namespace std;

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    short a;
    short b;
    short c;
    short d;
    cin >> a >> b >> c >> d;
    cout << a * b + c * d;
}